# 🛰️ Orbit CLI

The command-line interface for the Orbit Mini-PaaS.

## Commands

- `orbit login` - Authenticate with your dashboard account.
- `orbit deploy` - Push your current directory to Vercel/Netlify.
- `orbit tunnel <port>` - Expose a local port with a public URL.

## Installation

```bash
npm install -g @srizdebnath/orbit